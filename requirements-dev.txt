# Get git information if the ktbs was installed from git
GitPython==0.3.2.RC1
# For documentation purposes
Sphinx==1.2.2
# For developpers only
nose==1.3.3
pylint==1.2.1

# Make pip install the current project in editable mode (i.e. setuptools 
# "develop mode")
# That means that the files used are those of the cloned repository and 
# files retrived by further "git pull" will be used directly without any 
# install procedure. 
-e .
